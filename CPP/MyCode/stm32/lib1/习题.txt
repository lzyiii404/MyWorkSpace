1.新建工程-添加空白c/cpp文件-编写代码-编译-运行/debug。
2.scanf_s()比scanf()要安全；scanf_s()读取字符串时，必须提供
  一个数字以表明最多读取多少位字符，以防止溢出，而scanf没有。
3.使代码在不同编辑器环境中适配，避免因为代码缩进问题影响代码阅读。
4."#include <>"用于包含标准库的头文件，编译器会去系统配置的库环
  境变量和用户配置的路径去搜索，而"#include "xxx.h""会优先在项目
  当前目录搜索，无法识别后再去配置搜索。
5.在一个函数内部或复合语句内部定义的变量。eg：
                                          int main{
                                            int a;
                                            for(int i; ...;...)
                                          }
  整形 int ；短整型 short
6.在末尾加入死循环；在末尾加入输入scanf/cin。
