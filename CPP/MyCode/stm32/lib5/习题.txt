1.大端模式：将高字节存放于低地址，低字节存放于高地址；
  小端模式：将低字节存放于低地址，高字节存放于高地址。
2.指针对应着变量的地址；  数据类型+*+变量名(a)   则a为指针    
  a = &b;  //将a赋值为b的地址    *a ==  b;
3.数组是相连地址的集合，而数组变量则是这一连串地址的首地址，
  直接以使用指针的形式也可使用数组。
4.设置断点-进入调试模式-逐步调试
