#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n;
    vector<int> nums;
    nums.push_back(-1);
    cin >> n;
    for(int i = 0; i < n; i++){
        int tmp;
        cin >> tmp;
        nums.push_back(tmp);
    }
    int pos
}